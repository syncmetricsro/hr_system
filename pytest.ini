[pytest]
DJANGO_SETTINGS_MODULE = config.settings.local
python_files = test_*.py *_tests.py
testpaths = tests
addopts = --strict-markers
markers =
    jober_only: test asserts Jober-specific policies, URLs, or seed data; excluded from the corvinum-flags lane
